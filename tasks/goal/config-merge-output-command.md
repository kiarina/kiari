# merge 済み config yaml を出力するコマンドを追加する

## 背景

global config と profile config がどう merge されて最終的な設定値になるかを、
実行せずに確認する手段がない。設定の切り分けやサポート時に、merge 結果をそのまま
読める形で出したい。

## やること

- [ ] merge 済み config を yaml で標準出力へ書き出すコマンドを設計する
      （サブコマンド名、profile 指定方法、秘匿値の扱い）
- [ ] 実装し、公開境界のテストを追加する
- [ ] `README.md` の Usage へ追記する

## 進捗

未着手。着手条件なし（設定周りを触る機会に合わせて消化してよい）。

## 申し送り

- API キーなどの秘匿値をそのまま出力するかどうかは未決。既定でマスクし、明示的な
  flag で生値を出す形が無難だが、用途しだいで変わる
